import { StyleSheet } from "react-native";

export default StyleSheet.create({
  errorContainer: {
    marginTop: '60%',
    justifyContent: "center",
    alignItems: "center",
  },
  topBarContainer: {
    position: "absolute",
    left: 0,
    right: 0,
    zIndex: 1000,
  }
});